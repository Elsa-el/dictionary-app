import 'package:flutter/material.dart';

class FavoriteWordAdapter extends StatelessWidget {
  const FavoriteWordAdapter({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}